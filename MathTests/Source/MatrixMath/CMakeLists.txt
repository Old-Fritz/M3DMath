CMAKE_MINIMUM_REQUIRED(VERSION 3.8)

# define source dirs
SET(TESTS_MM_MATRIX_SRC_DIR ${CMAKE_CURRENT_SOURCE_DIR}/Matrix)

# find all sources
FILE(GLOB TESTS_MM_MATRIX_SRCS ${TESTS_MM_MATRIX_SRC_DIR}/*.cpp)

# add sources to target
TARGET_SOURCES(${MATH_TESTS_TARGET} PRIVATE ${TESTS_MM_MATRIX_SRCS})

